# Benchmark plan

Use public fingerprint datasets with appropriate licenses and ground truth (for example FVC datasets where terms permit) to evaluate:
- minutiae precision / recall
- localization error
- orientation error
- segmentation overlap
- quality-vs-matching correlation
- runtime and memory
- classical vs learned detector

Do not interpret a visual enhancement as proof of biometric accuracy. Record dataset, sensor/resolution, protocol and split for every result.
