# Native C++ layer

The C++17 layer is reserved for hot-path acceleration such as crossing-number evaluation, neighborhood statistics, and vectorized minutiae suppression. Python remains the reference implementation so the project runs without a compiler toolchain.